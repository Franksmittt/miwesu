I love where your head is at. You are absolutely right—building a "Rolls Royce" backend doesn't mean cramming it with a million confusing features. A true luxury tool is about **effortless power**. Wayne and Melissa don't want to need a computer science degree to run their lodge; they need something with Apple's clean, idiot-proof minimalism, combined with Samsung's rich, high-contrast, premium visual punch.

If we want this to be seamless and actually get used, the admin portal needs to be a single, unified command center that handles the enquiry, the communication, and the money, all tied into the existing Next.js and Resend stack.

Here is how we build a high-end, highly usable backend and a state-of-the-art Pricelist engine for MIWESU.

---

### 1. The "Rolls Royce" CRM & Booking Flow

To make this realistic for the owners, we keep the interface strictly focused on the guest pipeline. No clutter.

* **The Onyx/Gold Pipeline:** A visual Kanban-style or clean list dashboard showing **Pending**, **Quoted**, **Confirmed**, and **Upcoming Arrivals**.
* **The "One-Click" Quote:** When an enquiry comes in for The Homestead or The Stone Villa, Wayne or Melissa opens the thread. The system already knows the dates and guest count. The backend should allow them to click "Add Species/Activities" from a dropdown.
* **The Centralized Thread:** All emails sent to the client (pricing, T&Cs, bank details) are sent directly from this single view. No digging through Gmail.

### 2. The Dynamic "Master Pricelist" Engine

This is where we make their lives incredibly easy. We build a **Rates Manager** tab in the backend.

* **The Global Toggle:** A master switch at the top of the screen: **ZAR (R) | USD ($)**. They can set a global exchange rate, or manually override specific items to have fixed Dollar and Rand amounts.
* **Categorized Editing:**
* **Accommodations:** Easy input fields to update nightly rates for The Homestead (16 sleepers) and The Stone Villa (6 sleepers).
* **The 14 Species:** A clean, bento-style grid listing all species—from the Greater Kudu down to the Livingstone Eland. They can just click a price, type the new number, and hit save.
* **Experiences & Extras:** Fields for the Conservation Harvest fees, Photographic Safaris, and even the Miwesu Premium Firewood batches.


* **The Magic Integration:** Because this Master Pricelist is tied to the database, whenever they generate a quote or an invoice for a guest, the system automatically pulls the exact, up-to-date pricing. No manual typing required.

### 3. State-of-the-Art PDF Export

When they update their prices for the new season, they shouldn't have to hire a graphic designer. The backend will do it for them.

* **The Output:** They click a gold **"Generate Pricelist PDF"** button.
* **The Technology:** Since you already have `pdf-lib` integrated into the stack for the SAPS 520 form, we leverage that (or a server-side HTML-to-PDF tool like Puppeteer/Playwright) to instantly render a document.
* **The Aesthetic:** The PDF is generated using the site’s exact branding—Cinzel for the headings, Montserrat for the body, set against an Onyx and Gold palette. It looks like a high-end luxury brochure.
* **Client Ready:** It outputs a perfectly formatted, high-resolution document featuring the Iron Eden branding, their current ZAR or USD rates, and beautiful thumbnails of the species and lodges. They can instantly WhatsApp or email this directly to prospective hunters or agencies.

---

To pull this off, we need to structure the database so the prices are easy to fetch and update.

Would you like me to map out the exact Prisma database schema we'd need to power this Master Pricelist and tie it to the booking CRM?



Here is the exact, detailed prompt you can copy and paste directly into Cursor. It gives Cursor the full architectural vision, the exact tech stack to use, and the high-end design constraints (that Apple-meets-Samsung aesthetic) to make sure it builds exactly what you need without overcomplicating the codebase.

---

### Copy and paste this into Cursor:

**System Role & Context**
You are an expert Next.js 15, TypeScript, and Tailwind CSS developer. We are building a luxury, "Rolls Royce" tier Admin Backend and CRM for a game farm called MIWESU. The design language is extremely high-end: a fusion of Apple's clean minimalism and Samsung's vibrant, high-contrast visuals (Dark Onyx backgrounds, Liquid Glass panels, Cinzel & Montserrat fonts, and Gold 300-600 accents).

**The Goal**
We need to implement a lightweight, extremely user-friendly Admin Portal (`/admin`) that handles two main functions:

1. **A CRM Booking Pipeline:** Managing guest enquiries, sending emails via Resend, and generating quotes/invoices.
2. **A Master Pricelist Engine:** A dynamic rates manager where owners can effortlessly update prices for accommodations, 14 wildlife species, and activities, and then click a single button to generate a state-of-the-art, branded PDF Pricelist to send to clients.

Please execute this in the following phases. Do not use placeholder UI; build production-ready, beautiful interfaces.

**Phase 1: Prisma Schema Implementation**
Currently, Prisma is stubbed. We need to define the schema for this backend. Please create/update `schema.prisma` with the following models:

* `Booking`: id, guestName, email, phone, status (PENDING, QUOTED, CONFIRMED, CANCELLED), arrivalDate, departureDate, guestCount, residence (HOMESTEAD, STONE_VILLA), internalNotes, totalAmount, createdAt, updatedAt.
* `EmailLog`: id, bookingId (relation), subject, body, sentAt.
* `RateItem`: id, category (ACCOMMODATION, SPECIES, ACTIVITY, EXTRA), name (e.g., "Greater Kudu", "The Homestead"), priceZAR, priceUSD, isAvailable.
* `SystemSettings`: id, globalExchangeRate (ZAR to USD), lastUpdated.

**Phase 2: The Rates Manager UI (`/admin/rates`)**
Create a seamless, unified dashboard page where the owners can manage their prices.

* **Design:** Use a dark Onyx theme with Liquid Glass bento grids.
* **Features:** * A master toggle at the top to switch the view between **ZAR (R)** and **USD ($)**.
* Categorized sections: "Residences" (Homestead 16 sleepers, Stone Villa 6 sleepers), "Species" (listing all 14 species like Kudu, Buffalo, Sable, etc.), and "Experiences".
* Inline editing: Clicking a price allows instant updating via a server action or API route.
* A prominent, haptic-feedback enabled Gold button: **"Export Master Pricelist (PDF)"**.



**Phase 3: The State-of-the-Art PDF Generator (`/api/admin/generate-pricelist`)**
We need a visually stunning PDF generated dynamically from the `RateItem` database.

* **Tech:** Since we are using Next.js 15, implement a robust PDF generation solution (e.g., using `@react-pdf/renderer` or a serverless Puppeteer/Playwright setup to convert an HTML template to PDF). We already have `pdf-lib` in the project, but use the best tool for rendering complex, beautiful UI into a PDF.
* **Design of PDF:** It must not look like a spreadsheet. It should look like a luxury brochure. Use the Cinzel font for headings, dark backgrounds with gold text, and clean, perfectly aligned grids for the species and accommodation prices.
* **Function:** Fetch the latest ZAR and USD prices from the database, render the document, and return it as a downloadable file.

**Phase 4: The CRM Enquiry Thread (`/admin/bookings/[id]`)**
Build the individual booking view.

* **Layout:** Left column for Enquiry Details (Dates, Guests, Notes). Right column for the Email/Communication Thread.
* **Quoting Tool:** Add a "Create Quote" modal. This must pull data directly from the `RateItem` table (so they can select "The Homestead - 3 nights", "1x Blue Wildebeest", "1x Impala" from a dropdown) and instantly calculate the total in ZAR or USD.
* **Communication:** Integrate a rich-text input to send an email to the client via our existing Resend setup. The email should attach the generated quote.

Please start with Phase 1 (Prisma Schema) and ask for my approval before moving to Phase 2. Ensure all code adheres to our Next.js App Router and Tailwind best practices.

---

Would you like me to map out exactly what the `schema.prisma` file should look like so you can just hand that to Cursor as well?

Here is the ultimate master prompt and architectural blueprint you can feed directly into Cursor. It contains the exact database schema, the route structure, and the step-by-step instructions Cursor needs to build this "Rolls Royce" system.

Copy everything below the line and paste it into a new Cursor Composer session or a `cursor-plan.md` file.

---

# MIWESU Luxury CRM & Master Pricelist — Cursor Implementation Guide

**System Role:** You are an elite Next.js 15, TypeScript, Tailwind CSS, and Prisma developer. We are building a high-end, private Admin CRM and Master Pricelist generator for the MIWESU Game Farm.

**Design Language:** * **Palette:** Onyx (dark background), Marble (light sections), and Gold 300-600 gradients.

* **Typography:** Cinzel (serif headings) and Montserrat (sans-serif body).
* **UI Components:** Liquid Glass (translucent dark panels), Bento Grids, subtle Framer Motion reveals, and haptic feedback on primary actions.
* **Vibe:** A fusion of Apple's intuitive minimalism and Samsung's vibrant, high-contrast visual punch. It must feel like a luxury tool, not a spreadsheet.

Please execute the following build phases sequentially.

---

### Phase 1: Un-stub Prisma & Apply the Database Schema

Currently, Prisma is stubbed in the project. We need to enable it and apply a schema that supports both the CRM pipeline and the dynamic Master Pricelist.

Update `prisma/schema.prisma` with the following:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // or whichever DB provider MIWESU is using
  url      = env("DATABASE_URL")
}

// --- MASTER PRICELIST ENGINE ---

enum RateCategory {
  ACCOMMODATION
  SPECIES
  ACTIVITY
  EXTRA
}

model RateItem {
  id          String       @id @default(cuid())
  category    RateCategory
  name        String       // e.g., "The Homestead", "Greater Kudu", "Sekelbos Wood"
  description String?      // e.g., "Sleeps 16", "Trophy fee"
  priceZAR    Float
  priceUSD    Float
  isAvailable Boolean      @default(true)
  updatedAt   DateTime     @updatedAt
  
  // Relations for quoting
  quoteItems  QuoteItem[]
}

model SystemSettings {
  id                 String   @id @default("global")
  exchangeRateZarUsd Float    @default(18.50) // Master toggle conversion
  updatedAt          DateTime @updatedAt
}

// --- CRM & BOOKING PIPELINE ---

enum BookingStatus {
  PENDING
  QUOTED
  CONFIRMED
  CANCELLED
}

model Booking {
  id             String        @id @default(cuid())
  guestName      String
  email          String
  phone          String?
  arrivalDate    DateTime
  departureDate  DateTime
  guestCount     Int
  residence      String        // "The Homestead" or "The Stone Villa"
  status         BookingStatus @default(PENDING)
  internalNotes  String?       @db.Text
  createdAt      DateTime      @default(now())
  updatedAt      DateTime      @updatedAt

  quotes         Quote[]
  emailLogs      EmailLog[]
}

model Quote {
  id          String      @id @default(cuid())
  bookingId   String
  booking     Booking     @relation(fields: [bookingId], references: [id], onDelete: Cascade)
  currency    String      @default("ZAR") // "ZAR" or "USD"
  totalAmount Float
  validUntil  DateTime?
  createdAt   DateTime    @default(now())
  
  items       QuoteItem[]
}

model QuoteItem {
  id          String   @id @default(cuid())
  quoteId     String
  quote       Quote    @relation(fields: [quoteId], references: [id], onDelete: Cascade)
  rateItemId  String
  rateItem    RateItem @relation(fields: [rateItemId], references: [id])
  quantity    Int      @default(1)
  unitPrice   Float    // Locked-in price at time of quoting
}

model EmailLog {
  id          String   @id @default(cuid())
  bookingId   String
  booking     Booking  @relation(fields: [bookingId], references: [id], onDelete: Cascade)
  subject     String
  body        String   @db.Text
  sentAt      DateTime @default(now())
}

```

---

### Phase 2: The Rates Manager (`/admin/rates`)

Create the UI where owners manage prices for the 14 species, 2 residences, and activities.

1. **Layout:** Create `app/admin/rates/page.tsx`. Use a dark Onyx background with Liquid Glass cards for each category (Accommodations, Species, Activities).
2. **Global Currency Toggle:** Add a sleek toggle at the top to switch the UI between ZAR and USD, pulling the `exchangeRateZarUsd` from `SystemSettings`.
3. **Inline Editing:** Map over the `RateItem` data. Make the prices editable directly in the grid (using Next.js Server Actions to save `onBlur` or via a sleek "Save Changes" gold button).
4. **PDF Generation Trigger:** Place a premium, gold-gradient button: **"Generate Luxury Pricelist (PDF)"** at the top right.

---

### Phase 3: The State-of-the-Art PDF Generator (`/api/admin/pricelist-pdf`)

When the owner clicks the generate button, we need to create a gorgeous, branded PDF.

1. **Tech Choice:** Install `@react-pdf/renderer`. It is the best tool for building declarative, React-based PDFs in a Next.js environment. (Do not use `pdf-lib` for this, as `pdf-lib` is better for filling existing forms like the SAPS 520, whereas we need to render dynamic layouts here).
2. **Design the Template:** Create a component `components/pdf/MasterPricelistPDF.tsx`.
* Load the Cinzel and Montserrat fonts into the PDF context.
* Use a dark theme (simulated via dark background hex codes and gold text `color: '#D4AF37'`).
* Create clean, 2-column bento-style layouts for the 14 species and the accommodation rates.


3. **The API Route:** Create `app/api/admin/pricelist-pdf/route.ts`. Fetch all active `RateItem`s from Prisma, pass them as props to `<MasterPricelistPDF />`, render it to a stream using `renderToStream`, and return it with `Content-Type: application/pdf`.

---

### Phase 4: The Enquiry CRM & Quoting Tool (`/admin/bookings/[id]`)

Upgrade the booking detail view to be a full CRM workspace.

1. **Split View Layout:** * **Left Column (Guest File):** Guest details, dates, unit, internal notes.
* **Right Column (Comms & Finance):** A tabbed interface switching between "Email Thread" and "Quotes/Invoices".


2. **The Quoting Engine:** Under the "Quotes" tab, build a UI to create a new quote.
* Include a combobox/dropdown populated by the `RateItem` database.
* The owner selects "The Homestead (3 nights)" and "1x Blue Wildebeest", "1x Impala".
* The system auto-calculates the total.


3. **Resend Integration:** Use the existing Resend setup to send the quote directly from the portal, logging the message into the `EmailLog` table so the full conversation history is visible.

**Constraint Checklist & Confidence Score:**

1. Focus strictly on these 4 phases.
2. Ensure pristine TypeScript typing for all Prisma payloads.
3. Maintain the luxury Onyx/Gold aesthetic across all new Admin UI components.

---

Would you like me to also write out the exact `@react-pdf/renderer` code for the luxury PDF template, so you can give Cursor the exact styling logic for the document?




Let’s get this exactly right. To pull off the luxury aesthetic in a PDF, we can't rely on standard HTML-to-PDF converters that strip out styling. We are using `@react-pdf/renderer` because it allows us to build the PDF using React components and a Flexbox layout, giving us pixel-perfect control over the Onyx and Gold design system.

Here is the exact code block you need to feed into Cursor. It includes the necessary font registrations for Cinzel and Montserrat, the exact color palette, and the flexbox logic to create a premium, bento-style grid layout that doesn't look like a cheap spreadsheet.

---

### Copy and paste this into Cursor:

**File:** `components/pdf/MasterPricelistPDF.tsx`

**Context for Cursor:** This is the `@react-pdf/renderer` template for the MIWESU luxury pricelist. It must use a dark Onyx background (`#0A0A0A`) with Gold accents (`#D4AF37` and `#F3E5AB`). Do not use CSS Grid, as `@react-pdf/renderer` only supports Flexbox. We are registering the Cinzel and Montserrat fonts directly from Google Fonts so the PDF matches the web experience.

```tsx
import React from 'react';
import {
  Document,
  Page,
  Text,
  View,
  StyleSheet,
  Font,
  Image,
} from '@react-pdf/renderer';

// 1. Register Premium Fonts (Crucial for the luxury aesthetic)
Font.register({
  family: 'Cinzel',
  src: 'https://fonts.gstatic.com/s/cinzel/v19/8vIJ7ww63mVu7gtR-kwKxNvkbA.ttf',
});

Font.register({
  family: 'Montserrat',
  fonts: [
    { src: 'https://fonts.gstatic.com/s/montserrat/v25/JTUHjIg1_i6t8kCHKm4532VJOt5-QNF3Lw.ttf', fontWeight: 400 },
    { src: 'https://fonts.gstatic.com/s/montserrat/v25/JTUHjIg1_i6t8kCHKm4532VJOt5-QNF3Lw.ttf', fontWeight: 600 },
  ],
});

// 2. Define the exact Onyx & Gold stylesheet
const styles = StyleSheet.create({
  page: {
    backgroundColor: '#0A0A0A', // Deep Onyx
    color: '#E0E0E0', // Light Marble text for readability
    padding: 40,
    fontFamily: 'Montserrat',
  },
  headerContainer: {
    marginBottom: 30,
    borderBottomWidth: 1,
    borderBottomColor: '#D4AF37', // Gold 400
    paddingBottom: 20,
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'flex-end',
  },
  brandTitle: {
    fontFamily: 'Cinzel',
    fontSize: 28,
    color: '#D4AF37',
    letterSpacing: 2,
    textTransform: 'uppercase',
  },
  subtitle: {
    fontSize: 10,
    color: '#A0A0A0',
    marginTop: 4,
    letterSpacing: 1,
    textTransform: 'uppercase',
  },
  dateText: {
    fontSize: 10,
    color: '#D4AF37',
  },
  sectionTitle: {
    fontFamily: 'Cinzel',
    fontSize: 18,
    color: '#F3E5AB', // Lighter Gold for section headers
    marginTop: 20,
    marginBottom: 15,
    borderBottomWidth: 0.5,
    borderBottomColor: '#333333',
    paddingBottom: 5,
  },
  // Bento Grid emulation using Flex Wrap
  gridContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-between',
  },
  bentoCard: {
    width: '48%', // Two columns
    backgroundColor: '#141414', // Slightly lighter than background for Liquid Glass effect
    padding: 15,
    marginBottom: 15,
    borderRadius: 4,
    borderWidth: 1,
    borderColor: '#2A2A2A',
  },
  itemName: {
    fontFamily: 'Cinzel',
    fontSize: 14,
    color: '#FFFFFF',
    marginBottom: 8,
  },
  priceRow: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginTop: 4,
  },
  priceLabel: {
    fontSize: 10,
    color: '#888888',
  },
  priceValueZAR: {
    fontSize: 12,
    fontWeight: 600,
    color: '#E0E0E0',
  },
  priceValueUSD: {
    fontSize: 12,
    fontWeight: 600,
    color: '#D4AF37', // Gold for USD to pop
  },
  footer: {
    position: 'absolute',
    bottom: 30,
    left: 40,
    right: 40,
    textAlign: 'center',
    fontSize: 8,
    color: '#666666',
    borderTopWidth: 1,
    borderTopColor: '#2A2A2A',
    paddingTop: 10,
  }
});

// 3. Define the Types based on our Prisma Schema
type RateItem = {
  id: string;
  name: string;
  category: 'ACCOMMODATION' | 'SPECIES' | 'ACTIVITY' | 'EXTRA';
  priceZAR: number;
  priceUSD: number;
};

interface MasterPricelistPDFProps {
  items: RateItem[];
  generatedAt: string;
}

// 4. The PDF Component
export const MasterPricelistPDF = ({ items, generatedAt }: MasterPricelistPDFProps) => {
  const accommodations = items.filter(i => i.category === 'ACCOMMODATION');
  const species = items.filter(i => i.category === 'SPECIES');
  const activities = items.filter(i => i.category === 'ACTIVITY');

  const renderCard = (item: RateItem) => (
    <View style={styles.bentoCard} key={item.id}>
      <Text style={styles.itemName}>{item.name}</Text>
      <View style={styles.priceRow}>
        <Text style={styles.priceLabel}>ZAR</Text>
        <Text style={styles.priceValueZAR}>R {item.priceZAR.toLocaleString()}</Text>
      </View>
      <View style={styles.priceRow}>
        <Text style={styles.priceLabel}>USD</Text>
        <Text style={styles.priceValueUSD}>$ {item.priceUSD.toLocaleString()}</Text>
      </View>
    </View>
  );

  return (
    <Document>
      <Page size="A4" style={styles.page}>
        {/* Header */}
        <View style={styles.headerContainer}>
          <View>
            <Text style={styles.brandTitle}>MIWESU</Text>
            <Text style={styles.subtitle}>Iron Eden • Official Rates</Text>
          </View>
          <Text style={styles.dateText}>Valid As Of: {generatedAt}</Text>
        </View>

        {/* Accommodations Section */}
        {accommodations.length > 0 && (
          <View>
            <Text style={styles.sectionTitle}>Private Residences</Text>
            <View style={styles.gridContainer}>
              {accommodations.map(renderCard)}
            </View>
          </View>
        )}

        {/* Species Section */}
        {species.length > 0 && (
          <View>
            <Text style={styles.sectionTitle}>Conservation Harvest (Species)</Text>
            <View style={styles.gridContainer}>
              {species.map(renderCard)}
            </View>
          </View>
        )}

        {/* Footer */}
        <Text style={styles.footer}>
          MIWESU Game Farm • Makoppa District, Thabazimbi, South Africa • All rates subject to change without prior notice.
        </Text>
      </Page>
    </Document>
  );
};

```

---

This gives you an incredibly solid, exceptionally styled foundation. Would you like me to map out the exact API Route (`/api/admin/pricelist-pdf/route.ts`) that will take this component, fetch the live Prisma data, and serve it as a downloadable file when Wayne or Melissa clicks the "Download" button?